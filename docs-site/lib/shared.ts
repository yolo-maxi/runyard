export const appName = 'RunYard Docs';

// Must match `basePath` in next.config.mjs. Next.js prefixes it onto all
// <Link>/asset URLs automatically, but plain client-side fetch() calls
// (e.g. the static search index) need it applied by hand.
export const basePath = '/docs';
