# Standalone package: not part of the root pnpm project.
node_modules/
.next/
# Generated by fumadocs-mdx (postinstall / build)
.source/
next-env.d.ts
*.tsbuildinfo

# out/ (the static export) is COMMITTED: production serves it directly from
# the working tree without building, and repo.box never builds anything.
# Rebuild with `pnpm build:docs` from the repo root after editing content.
