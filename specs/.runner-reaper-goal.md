Implement the feature in /home/xiko/smithers-hub on branch feat/runner-reaper.
READ FIRST and follow exactly: /home/xiko/smithers-hub/specs/runner-identity-reaper.md
Read the existing code before editing: src/db.js (registerRunner ~1627, isRunnerLive ~1669, listRunners ~1703, heartbeatRunner ~1707, reapStuckRunIds ~1473), src/server.js (the reaper interval that calls reapStuckRunIds, /api/runners routes ~3591), src/smithers-runner.js (register call + workspace path), src/env.js (env vars like runnerOfflineMs), public/app.js (runners view + auth-health strip, primaryActionBar offline count).
Implement all 3 parts + tests. Run the eval gates until ALL pass. Do NOT push/deploy/merge — commit to feat/runner-reaper and stop. End with the SUMMARY from the spec.
Be decisive and autonomous; do not ask questions. Keep working until gates are green or a documented blocker.
